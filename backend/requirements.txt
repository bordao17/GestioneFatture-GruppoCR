# Comunicazione con il motore AI locale
ollama>=0.2.0

# PyMuPDF: Estrazione nativa ad altissima qualità dei PDF (importato come 'fitz')
PyMuPDF>=1.24.0

# Server API e Web Framework
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
pydantic>=2.0.0
Pillow
tzdata

# Sbustamento delle fatture elettroniche firmate (.xml.p7m): pura ASN.1, niente
# compilazione. Regge anche le buste BER che un parser fatto in casa romperebbe.
asn1crypto>=1.5.1

# Anagrafiche su Postgres (src/comune/database.py). Il driver "binary" evita
# di dover compilare libpq nell'immagine slim: niente gcc, niente libpq-dev.
psycopg[binary]>=3.1
